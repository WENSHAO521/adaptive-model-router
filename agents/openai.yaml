interface:
  display_name: "Adaptive Model Router"
  short_description: "Automatic routing, paper-writing delegation, and escalation"
  default_prompt: "Apply automatic model routing, bounded delegation, evidence-first paper workflows, and final validation."
policy:
  allow_implicit_invocation: true
