interface:
  display_name: "Adaptive Model Router"
  short_description: "Automatic routing, paper-writing delegation, and escalation"
  default_prompt: "Use $adaptive-model-router for automatic model routing, bounded delegation, and final validation."
policy:
  allow_implicit_invocation: true
